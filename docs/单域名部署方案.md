# 单域名部署方案（WordPress 风格）

## 🌐 部署结构

```
yourdomain.com/              → 前端（用户访问）
yourdomain.com/admin/        → 管理后台
yourdomain.com/api/          → 后端 API
```

**优点：**
- ✅ 只需一个域名
- ✅ 无需配置子域名
- ✅ 类似 WordPress 的访问方式
- ✅ 便于管理和维护

---

## 📁 宝塔目录结构

```
/www/wwwroot/yourdomain.com/
├── index.html              # 前端入口
├── static/                 # 前端静态资源
├── admin/                  # 管理后台
│   ├── index.html
│   └── assets/
└── api/                    # 后端（通过反向代理）
```

---

## 🔧 Nginx 配置

在宝塔面板 → 网站 → 配置文件中添加：

```nginx
server {
    listen 80;
    server_name yourdomain.com;
    root /www/wwwroot/yourdomain.com;
    index index.html;

    # 前端（根路径）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 管理后台
    location /admin {
        alias /www/wwwroot/yourdomain.com/admin;
        try_files $uri $uri/ /admin/index.html;
    }

    # 后端 API（反向代理到 Node.js）
    location /api {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态资源缓存
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
```

---

## 📦 部署步骤

### 1. 构建前端

```bash
cd frontend
# 修改 .env
REACT_APP_API_URL=/api
npm run build
```

### 2. 构建管理后台

```bash
cd admin
# 修改 .env
VITE_API_URL=/api
npm run build
```

### 3. 上传文件

```bash
# 前端文件上传到
/www/wwwroot/yourdomain.com/

# 管理后台文件上传到
/www/wwwroot/yourdomain.com/admin/

# 后端代码上传到
/www/wwwroot/yourdomain.com/backend/
```

### 4. 启动后端

```bash
cd /www/wwwroot/yourdomain.com/backend
npm install --production
npm run prisma:generate
pm2 start src/index.js --name uied-api
```

---

## 🎯 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 用户前端 | https://yourdomain.com | 主网站 |
| 管理后台 | https://yourdomain.com/admin | 内容管理 |
| API 接口 | https://yourdomain.com/api | 后端接口 |

---

## 💾 资源占用

**实测数据：**
- Node.js 进程：~50MB 内存
- SQLite 数据库：~1MB 磁盘
- 总体占用：可忽略不计

**对比：**
- WordPress + MySQL：~200MB 内存
- 本方案：~50MB 内存

**结论：** 对服务器性能几乎无影响！

---

## 🔒 安全建议

1. **隐藏管理后台入口**（可选）
```nginx
# 将 /admin 改为自定义路径
location /my-secret-admin {
    alias /www/wwwroot/yourdomain.com/admin;
    try_files $uri $uri/ /my-secret-admin/index.html;
}
```

2. **IP 白名单**（可选）
```nginx
location /admin {
    allow 123.123.123.123;  # 你的 IP
    deny all;
    # ...
}
```

---

## 📊 性能对比

| 方案 | 域名数量 | 内存占用 | 维护难度 |
|------|---------|---------|---------|
| 单域名（推荐） | 1 | ~50MB | 简单 |
| 子域名方案 | 3 | ~50MB | 中等 |
| 纯前端 | 1 | 0 | 最简单 |

**推荐：** 单域名方案，性价比最高！
