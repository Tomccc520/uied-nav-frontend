# 生产环境配置示例
# 复制此文件为 .env 并修改配置

# 数据库配置（SQLite 数据库文件路径）
# 注意：使用相对于 prisma 目录的路径
DATABASE_URL="file:./dev.db"

# 服务配置
PORT=3001
NODE_ENV=production

# 安全配置 - 必须修改！
# JWT_SECRET: 使用 openssl rand -hex 64 生成
JWT_SECRET="your-64-character-random-string-here"
JWT_EXPIRES_IN="24h"

# PASSWORD_SALT: 使用 openssl rand -hex 32 生成
PASSWORD_SALT="your-32-character-random-string-here"

# CORS 配置 - 设置允许的前端域名（单域名部署只需要一个）
CORS_ORIGINS="https://yourdomain.com"

# 前端地址（SEO 功能需要，用于生成 sitemap）
FRONTEND_URL="https://yourdomain.com"
